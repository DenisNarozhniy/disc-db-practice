import { HomeComponent } from './components/home-component/home.component';
import { homeRoutesNames } from './home.route.names';

export const HomeRoutes = [
    {
        path: homeRoutesNames.Home,
        component: HomeComponent
    }
];
